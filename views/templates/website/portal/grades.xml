<?xml version="1.0" encoding="utf-8"?>
<!--
/**
 * @file grades.xml
 * @brief Portal template for displaying student grades
 *
 * This template contains:
 * - Portal home extension for grades access
 * - Detailed grades view with filtering options
 * - Responsive table for grades display
 *
 * Features:
 * - University and grade status filtering
 * - Admin view with student information
 * - Dynamic grade styling based on pass/fail
 * - Date formatting
 * - Empty state handling
 *
 * @version 1.0
 * @date 2025-04-25
 */
-->
<odoo>
    <!-- Portal My Home Extension -->
    <template id="portal_my_home_inherit" inherit_id="portal.portal_my_home" name="Portal - Grades Link">
        <xpath expr="//div[contains(@class, 'o_portal_my_home')]" position="inside">
            <t t-if="request.env['university.student'].sudo().search([('user_id', '=', request.env.user.id)], limit=1)">
                <div class="card mb-3">
                    <div class="card-body">
                        <h5 class="card-title">View My Grades</h5>
                        <a href="/my/grades" class="btn btn-primary">Access Grades</a>
                    </div>
                </div>
            </t>
        </xpath>
    </template>

    <!-- Grades Portal View -->
    <template id="portal_grades" name="Portal - My Grades">
        <t t-call="portal.portal_layout">
            <div class="container py-5">
                <!-- Header and Filters -->
                <div class="card shadow-sm mb-4">
                    <div class="card-body">
                        <div class="row align-items-center">
                            <div class="col-md-6">
                                <h2 class="h4 mb-md-0">
                                    <t t-if="is_admin">All Grades</t>
                                    <t t-else="">My Grades</t>
                                </h2>
                            </div>
                            <div class="col-md-6">
                                <form class="d-flex gap-3" method="GET">
                                    <select name="university_id" class="form-select" onchange="this.form.submit()">
                                        <option value="">All Universities</option>
                                        <t t-foreach="universities" t-as="uni">
                                            <option t-att-value="uni.id" 
                                                    t-att-selected="uni.id == current_university">
                                                <t t-esc="uni.name"/>
                                            </option>
                                        </t>
                                    </select>
                                    <select name="grade_filter" class="form-select" onchange="this.form.submit()">
                                        <option value="all" t-att-selected="current_filter == 'all'">
                                            All Grades
                                        </option>
                                        <option value="passed" t-att-selected="current_filter == 'passed'">
                                            Passed
                                        </option>
                                        <option value="failed" t-att-selected="current_filter == 'failed'">
                                            Failed
                                        </option>
                                    </select>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Grades Table -->
                <div class="card shadow-sm">
                    <div class="table-responsive">
                        <table class="table mb-0" style="--bs-table-hover-bg: transparent;">
                            <thead class="table-light">
                                <tr>
                                    <t t-if="is_admin">
                                        <th>Student</th>
                                    </t>
                                    <th>Subject</th>
                                    <th>Professor</th>
                                    <th>University</th>
                                    <th>Date</th>
                                    <th class="text-center">Grade</th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="grades" t-as="grade">
                                    <tr>
                                        <t t-if="is_admin">
                                            <td><t t-esc="grade.student_id.name"/></td>
                                        </t>
                                        <td><t t-esc="grade.enrollment_id.subject_id.name"/></td>
                                        <td><t t-esc="grade.enrollment_id.professor_id.name"/></td>
                                        <td><t t-esc="grade.enrollment_id.university_id.name"/></td>
                                        <td><t t-esc="grade.enrollment_id.date.strftime('%d/%m/%Y')"/></td>
                                        <td t-attf-class="text-center">
                                            <span t-attf-class="badge rounded-pill fs-6 #{grade.grade >= 5.0 and 'bg-success' or 'bg-danger'}">
                                                <t t-esc="'%.2f' % grade.grade"/>
                                            </span>
                                        </td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                    </div>

                    <!-- No Data Message -->
                    <div t-if="not grades" class="card-body text-center py-5">
                        <div class="text-muted">
                            <i class="fa fa-info-circle fa-2x mb-3"></i>
                            <p>No grades available at this time.</p>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>
</odoo>